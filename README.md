# Pulag — landing page

The public marketing site for Pulag: the trust layer for mining's orebody data.

Static, self-contained (vendored OFL fonts, no external requests). Served via
GitHub Pages from `main`.
